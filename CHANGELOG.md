## [unreleased]

### 💼 Other

- Initial commit
- Primeira versão do script.
- Criando um arquivo de teste para certos protótipos. No caso, estou testando uma nova versão do módulo LVM que organiza os volumes e deixa a função 'fetchVolumes' dinâmica com base em códigos. Ainda não finalizado.
- Criada a função que verifica a viabilidade da execução de snapshot baseada em um espaço de até 20% de sobra do volume group em que o logical volume é baseado. Claro, fiz a função com base em tamanhos de GigaByte, ainda falta para MegaByte e talvez TeraByte.
- Tirando os 'ifs' e colocando uma estrutura 'case' na função 'fetchVolumes'. Além de tirar alguns comentários de teste dentro da função.
- Criando um menu para o usuário escolher algumas operações. Ainda testando e pensando como chamarei cada função. Algumas linhas foram remontadas para uma melhor visualização do código. Comentando a linha da criação do array 'lvs' no arquivo 'lvm.sh' pois o array ainda não está sendo utilizado em nenhum lugar do projeto.
- Dando uma reformulada no código do arquivo 'lvm.sh'. Adicionei um mecanismo que associa o LV ao seu respectivo VG ao invés de depender da quantidade e ordem das tabelas das variáveis 'raw'. Diminuí um pouco o 'if' que determina a viabilidade do snapshot ou não.
- Adicionando um cabeçalho que identifica cada arquivo do projeto via comentário. mudando o valor da variável temporaria 'IFS' no início de 'lvm.sh' para que pare de dar problemas de quebra de linha. Criando uma nova função de impressão chamada 'print' em 'view.sh' para que toda a responsabilidade de output seja do módulo da View.
- Adicionando uma verificação da linguagem local do SO.
- Começando a adicionar uma lógica de detecção de arquivo temporário de erro. Ainda preciso de alguns casos de erro. Dediquei mais tempo à máquina virtual de testes do que o código. Adicionei uma linha que redireciona todas as mensagens de erro do processo de update do 'pacman' a um arquivo temporário. Coloquei um 'grep' no Controller para ver se a linguagem do sistema operacional do usuário está em inglês.
- Mudando a forma como as mensagens que aparecem na tela são organizada no código por meio od Here Documents.
- Colocando uma verificação de internet no script. Criando uma função no módulo de log para verificar se o arquivo de erro existe.
- Corrigindo o calculo da função do 'snapshotViability'
- Mudando a lógica do cálculo da viabilidade do snapshot. Nesse caso, a função 'snapshotViability' passa a considerar a soma do espaço ocupado do(s) volume(s) lógico(s) no sistema e compara com o tamanho mínimo do VG para que um snapshot seja realizado.
- Colocando a coluna 'MINIMAL_FOR_SNAPSHOT'...
- Mudando um pouco a estrutura do projeto.
- Começando a colocar um desenho de tabela para informar a quantidade de volumes e grupos de volumes na função 'snapshotViability'
- Criando uma nova lógica de ordenação em tabela. Com os cabeçalhos na lateral esquerda e a soma dos lvs e vgs. Ainda falta muito.
- Mudando um pouco a variável temporária e adicionando uma tabelinha no final da função 'snapshotViability'
- Iniciando uma função para criação e padronização de tabelas para o output do usuário. Além de corrigir um erro que ocorria no vetor 'completeTable' em que uma posição vinha vazia por um erro de lógica de um dos 'fors' da função 'snapshotViability'
- Detalhando mais a função 'table' da View, adicionando o mecanismo de lógica dinâmica baseada em código passado por parâmetro. Removendo a função 'print' da View pois considero que é redundante.
- Implementando uma forma de criar cabeçalho para a tabela de forma que a largura da linha fique adaptado ao texto do cabeçalho. E também reorganizando um pouco os arquivos.
- Adicionando a formatação de tabela do output vindo da função 'snapshotViability' do arquivo 'lvm.sh'. E dando uma reformulada no Controller para tirar alguns caracteres desnecessários.
- Corrigindo o cálculo da largura da linha da tabela.
- Commitando para testar nas VMs os logs de erro.
- Ajeitando um pouco o cálculo do espaço do VG e VG Free.
- Criando a função para verificar os binários em '/usr/bin' a fim de saber quais binários possuem libs faltando.
- Mudando a forma com que o sistema determina se o SO está ou não com conexão coma  internet.
- Iniciando a construção do módulo de 'debug'. Ainda preciso determinar como e onde passar as mensagens. Deixei alguns exemplos na função 'debug()' para nortear a minha linha de raciocínio.
- Implementando o novo módulo de depuração (DEBUG). Nos arquivos 'controller.sh', 'lvm.sh' e 'log.sh' a função 'debugPrint' é adicionada caso a variável 'DEBUG' for igual a 0.
- Corrigindo algumas partes do código.
- Adicionando uma organização em tabela na função verifyBinaries alinhando os binários em '/usr/bin' com as respectivas libs não encontradas.
- Implementando mais mensagens de depuração no script, principalmente no arquivo 'lvm.sh'. Consideranda arquitetura escolhida, onde o Controller é quem orquestra tudo, decidi deixar as chamadas da função 'fetchVolumes' para a função 'main' no Controller ao invés do 'snapshotViability' no módulo LVM. Tirando aquele print besta que coloquei do 'update.sh' e deixei uma mensagem de erro genérica. No módulo de DEBUG, adicionei, na função 'debugPrint', a data e hora de cada mensagem de depuração, a fim de facilitar o diagnóstico. Na função 'fetchVolumes' adicionei as variáveis 'LVM_SUPPRESS_FD_WARNINGS' para que não apareçam os erros do LVM do Linux, não é nada permanente e devo estudar mais a fundo sobre esses erros de 'memory leak'.
- Adicionando registros de início e fim do processo de atualização no módulo UPDATE. Colocando mais mecanismos de leitura de log via 'journalctl' e consulta direta ao arquivo 'pacman.log'
- Retornando a forma original da função 'snapshotViability' de receber os argumentos da função 'fetchVolumes'
- Reformulando a forma da consulta dos logs do 'journalctl'. Adicionando uma verificação no módulo UPDATE para caso a atualnão haja atualizações disponíveis. Adicionando a chamada das funções de verificação dos logs do sistema e do pacman no CONTROLLER. E adicionando uma verificação que determina que se a viabilidade de snapshot for impossível, o sistema deve parar imediatamente.
- Adicionando o menu de escolha de 'COMMIT' e 'ROLLBACK' na etapa final do script.
- Começando a formular a função de criação de snapshot em 'lvm.sh'. Commitando para testar na VM.
- Implementando a função de criação de snapshot com base nos 20% de espaço de volume vagos. Também implementando uma função que exclui o snapshot.
- Implementando a função de rollback do snapshot. Ainda falta testar mais, mas o rollback e o reboot já estão sendo executados sem problemas.
- Commitando depois de alguns testes.
- Commitando para dar o push
- Adicionando um mecanismo de criação de serviço e timer via systemD para que o snapshot seja deletado após 3 dias da decisão do usuário de dar commit.
- Efetivando a criação do timer via systemD.
- Dando uma limpada no código do projeto.
- Colocando todas as operações de gerenciamento de snapshot na função 'snapshotManagement'
- Juntando todas as funções do módulo de 'DEBUG' a fim de centralizar o controle de processos.
- Mudando a forma como o log gerado a partir do CONTROLLER é gerado.
- Dando uma melhorada na função de criação de tabela dos volumes que o script detectou.
- Colocando algumas verificações de LVM e SystemD no sistema do usuário. Ainda falta testar.
- Adicionando uma averiguação dos pacotes para caso o kernel atualize e o diretório '/boot' seja mudado a fim de que, no rollback, o reboot seja sucedido.
- Implementando a criação de um gatilho de inicialização em que, após o rollback, os snapshots persistentes são apagados completamente.
- Adicionando uma verificação do tipo de distribuição Linux que o usuário esteja usando.
- Adicionando mensagens mais visíveis para que o usuário saiba qual informação pertence a qual parte. Só tá bugando um pouco na mensagem final do COMMIT, mas já resolvo...
- Implementando uma forma de conversão de megabytes para gigabytes caso algum dos tamanhos dos vgs estejam em MB. Além disso, foi implementado a criação do snapshot com base no VG com mais espaço.
- Realizando alguns ajustes a respeito do BC no lvm.sh que tava bugando o cálculo dos LVs.
- Feito algumas alterações. Mexendo um pouco na geração do timer de exclusão do snapshot. Testado com sucesso.
- Realizando algumas correções. Adicionando cores para destaque no terminal no DEBUG e VIEW.
- Dando uma apagada nos comentários do script.
- Adicionando um README mínimo.
- Adicionando mecanismos de syscalls nos módulos CONTROLLER, UPDATE e LVM.
- Adicionando um terminal ainda mais interativo com base nas setas do teclado. Ainda não tá 100% porque apaga todo o resto do log...
- Adicionando um título de introdução mais diferenciado para a função 'intro' da VIEW.
- Implementando uma forma melhor de tirar caracteres especiais do arquivo de log. Reformulando o código do script tirando alguns ifs e deixando menos espaçoso o código.
- Tirando uma verificação de erro no UPDATE. Vou lançar essa versão para a branch 'main' logo.
- Merge pull request #2 from JVBC743/test

Realizando o merge para lançar a 1° versão do script.
- Começando a adicionar mecanismos para que o script gerencie os snapshots com base na quantidade de volumes lógicos. Ainda tá faltando terminar a parte do rollback e delete do snapshot.
- Commitando para mudar de branch
- Separando o CONTROLLER por fases a fim de melhorar a manutenção e localização de erros. Sofisticando mais o processo de exclusão do(s) snapshot(s) criados em caso de não haver atualizações disponíveis no UPDATE e CONTROLLER.
- Implementando uma forma de excluir mais de um volume em caso de não ter updates em um ambiente com mais de um volume.
- Implementando o rollback para ambientes com mais de um volume lógico.
- Implementando um mecanismo de avaliação individual para cada VG e LV a respeito da viabilidade do snapshot baseado no VG do LV. E, por algum motivo, a variável 'sumFreeVG' não estava sendo usada, o que estava limitando muito o cálculo.
- Implementando a formatação em tabela do resultado da viabilidade de snapshot, com cor...
- Adicionando, no banner inicial, uma formatação em tabela que contém a versão do sistema.
- Alterando a informação do VG no arquivo de limpeza que é engatilhado no ambiente pós-rollback.
- Adicionando a unidade de medida 'g' na coluna que contém os valores que indicam o mínimo para o snapshot em LVM.
- Colocando a palavra-chave 'function' em todas as funções do sistema para que seja mais fácil de identificar na pesquisa. Começando a criar uma forma mais sofisticada para a criação dos snapshots em ambientes com mais de um VG.
- Corrigindo a forma com que o cleaner é removido em caso do ambiente nào ser propício para a criação de snapshots. Reformulando a forma com que os snapshots são criados e excluídos com base nos VGs dos LVs.
- Implementando o mecanismo de persistência de output no menu de escolha entre o 'commit' e 'rollback'
- Corrigindo o processo de rollback onde o grupo de volumes não era especificado.
- Adicionando uma tela interativa para a vistoria do arquivo de log como uma opção no segundo menu junto com as opções de 'commit' e 'rollback' no CONTROLLER. Corrigindo o erro do printf onde ele reclamava do uso do 'G' no final da formatação dos números floats no LVM. Adicionando mais verificações de erro em cada função chamada na 'main' no CONTROLLER. Adicionando a visualização da tabela de viabilidade de snapshot no fluxo comum do script na função 'preUpdate' no CONTROLLER.
- Realizando a correção do retorno da função de 'update' em CONTROLLER. Adicionando uma forma de verificação do comando 'less' no Linux do usuário.
- Adicionandoo uma forma de filtrar apenas os volumes (sem considerar snapshots) na função 'fetchVolumes' em LVM.
- Corrigindo o erro de filtração de volumes. Agora a filtragem é usando o awk para separar os volumes já existentes do sistema dos volumes que sáo criados pelo script. Adicionando o nome do autor do script no banner de boas-vindas. hihi.
- Adicionando o nome do dia da semana no aviso do commit e o horário específico da exclusão do snapshot.
- Corrigindo o erro onde a condição que para o script em caso do ambiente não possuir o LVM. Aliás, coloquei o return em todas as condições que fazem parte do mesmo contexto.
- Merge pull request #3 from JVBC743/test

Releasing a newer version for the main branch
